def test_example(page: Page) -> None:
    page.get_by_role("button", name="Wybór sklepu").click()
    page.get_by_role("button", name="hit_bazar ✅ Nie wymaga połą").click()
    page.get_by_role("button", name="Wybór sklepu").click()
    page.get_by_role("button", name="radosnydzieciak ✅ Nie wymaga").click()
    page.get_by_role("button", name="Wybór sklepu").click()
    page.get_by_role("button", name="skarbiec_ofert ✅ Nie wymaga").click()
    page.get_by_role("button", name="Twoje sklepy").click()
    page.get_by_role("link", name="Produkty", exact=True).click()
    page.get_by_placeholder("Od").first.click()
    page.get_by_placeholder("Od").first.fill("100")
    page.get_by_role("button", name="Czechy (CZK)").click()
    page.get_by_role("button", name="Polska PLN").click()
    page.get_by_placeholder("Od").nth(1).click()
    page.get_by_placeholder("Od").nth(1).fill("0")
    page.locator("div:nth-child(2) > div > .relative > .w-full > .absolute.top-\\[3px\\] > .lucide").first.click()
    page.get_by_role("button", name="Wszystkie dostępne produkty").click()
    page.get_by_placeholder("Od").nth(1).click()
    page.get_by_placeholder("Od").nth(1).fill("2")
    page.get_by_role("button", name="Hurtownia").click()
    page.get_by_role("button", name="4KOM (8395 na stanie)").click()
    page.get_by_role("button", name="4KOM (8395 na stanie)").click()
    page.get_by_role("button", name="Abisal (1936 na stanie)").click()
    page.get_by_role("button", name="Abisal (1936 na stanie)").click()
    page.get_by_role("button", name="Action (24142 na stanie) 🟡").click()
    page.get_by_role("button", name="Action (24142 na stanie) 🟡").click()
    page.get_by_role("textbox", name="Szukaj opcji...").click()
    page.get_by_role("textbox", name="Szukaj opcji...").fill("4")
    page.get_by_role("textbox", name="Szukaj opcji...").press("CapsLock")
    page.get_by_role("textbox", name="Szukaj opcji...").press("CapsLock")
    page.get_by_role("textbox", name="Szukaj opcji...").press("CapsLock")
    page.get_by_role("textbox", name="Szukaj opcji...").fill("4KOM")
    page.get_by_role("button", name="4KOM (8395 na stanie)").click()
    page.get_by_role("button", name="4KOM (8395 na stanie)").click()
    page.get_by_role("textbox", name="Szukaj opcji...").click()
    page.get_by_role("textbox", name="Szukaj opcji...").fill("")
    page.get_by_role("textbox", name="Szukaj opcji...").press("CapsLock")
    page.get_by_role("textbox", name="Szukaj opcji...").fill("Action")
    page.get_by_role("button", name="Action (24142 na stanie) 🟡").click()
    page.get_by_role("button", name="Action (24142 na stanie) 🟡").click()
    page.get_by_role("textbox", name="Szukaj opcji...").click()
    page.get_by_role("textbox", name="Szukaj opcji...").fill("AmpSource")
    page.get_by_role("button", name="AmpSource (8036 na stanie)").click()
    page.get_by_role("checkbox", name="Zaznacz wszystkie na tej").check()
    page.get_by_role("button", name="Wyślij zaznaczone produkty").click()
    page.get_by_role("button", name="🏪 Wyślij produkty na sklep").click()
    page.get_by_role("button", name="📦 Wyślij produkty na").click()
    page.get_by_role("button", name="Wybierz sklep i konto Allegro").click()
    page.get_by_role("button", name="radosnydzieciak | PROD |").click()
    page.get_by_role("button", name="radosnydzieciak | PROD |").click()
    page.get_by_role("button", name="skarbiec_ofert | PROD |").click()
    page.get_by_role("button", name="skarbiec_ofert | PROD |").click()
    page.get_by_role("button", name="hit_bazar | PROD | hit_bazar").click()
    page.locator(".w-full.px-3.py-2.bg-slate-700").first.click()
    page.locator(".w-full.px-3.py-2.bg-slate-700").first.fill("100")
    page.get_by_role("button", name="📦 Wyślij na Allegro ({100}").click()
    page.get_by_role("spinbutton", name="Wpisz numer strony (1-{52}) i").first.click()
    page.get_by_role("spinbutton", name="Wpisz numer strony (1-{52}) i").first.fill("2")
    page.get_by_role("button", name="Przejdź do strony").first.click()
    page.get_by_role("checkbox", name="Zaznacz wszystkie na tej").check()
    page.get_by_role("button", name="Wyślij zaznaczone produkty").click()
    page.get_by_role("button", name="📦 Wyślij produkty na").click()
    page.get_by_role("button", name="📦 Wyślij produkty na").nth(1).click()
    page.get_by_role("button", name="hit_bazar | PROD | hit_bazar").click()
    page.get_by_role("button", name="hit_bazar | PROD | hit_bazar").nth(1).click()
    page.locator(".w-full.px-3.py-2.bg-slate-700").first.click()
    page.locator(".w-full.px-3.py-2.bg-slate-700").first.fill("100")
    page.get_by_role("button", name="📦 Wyślij produkty na").click()
    page.get_by_role("button", name="📦 Wyślij produkty na").nth(1).click()
